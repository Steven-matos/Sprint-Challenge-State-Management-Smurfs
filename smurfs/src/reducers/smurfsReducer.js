
const initialState = {
    smurfs: [],
    isFetchingData: false,
    error: ''
}

export const smurfsReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};